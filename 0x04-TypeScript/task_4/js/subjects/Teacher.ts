/* Create a file Teacher.ts and write a Teacher interface in a namespace
named Subjects.

the interface requires firstName and lastName as string */

namespace Subjects {
  export interface Teacher {
    firstName: string;
    lastName: string;
  }
}
